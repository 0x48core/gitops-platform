apiVersion: batch/v1
kind: Job
metadata:
  name: handle-sync-failure
  annotations:
    argocd.argoproj.io/hook: SyncFail
spec:
  template:
    spec:
      containers:
        - name: cleanup
          image: my-app:latest
          command: ["./cleanup-on-failure.sh"]
      restartPolicy: Never